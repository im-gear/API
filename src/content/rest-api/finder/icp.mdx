# ICP (Ideal Customer Profile)

## Endpoints

### Get ICP Data
`GET /api/finder/icp`

Loads ICP mining data and the associated role query.

**Query Parameters:**
- `icp_id` (UUID, required): The ID of the ICP.
- `site_id` (UUID, optional): The ID of the site.
